// ignore: file_names
import 'package:flutter/material.dart';

class PageSettingsProfile extends StatelessWidget {
  const PageSettingsProfile({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}